Django Boardinghouse
====================

Multi-tenancy for Django applications, using Postgres Schemas.

See full documentation at: http://django-boardinghouse.readthedocs.org

This is an experimental branch that swaps over the default of shared models to a philosophy that you need to explicitly declare which models are shared.